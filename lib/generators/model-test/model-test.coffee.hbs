{{#camelize}}{{name}}{{/camelize}}= require 'models/{{name}}'

describe '{{#camelize}}{{name}}{{/camelize}} Model', ->
  beforeEach ->
    @model= new {{#camelize}}{{name}}{{/camelize}}()
    @collection= new {{#camelize}}{{name}}{{/camelize}}.Collection()

  afterEach ->
    @model.dispose()
    @collection.dispose()

  it 'should exist', ->
    expect(@model).to.exist
    expect(@collection).to.exist

