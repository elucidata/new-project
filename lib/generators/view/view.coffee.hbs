View = require 'framework/view' # or 'framework/collection-view'
template = require 'views/templates/{{name}}'

module.exports = class {{#camelize}}{{name}}{{/camelize}}View extends View
  template: template
