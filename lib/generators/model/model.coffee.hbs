Model= require 'framework/model'
Collection= require 'framework/collection'
UndoManager= require 'framework/undo-manager'


class {{#camelize}}{{name}}{{/camelize}} extends Model
  # Enables automatic updatedOn/createdOn tracking
  # @trackTimestamps()

  # Define your attribute methods here: (all options are optional)
  # @attr 'attributeName', default:'Default Value', alias:'attributeName', property:no


{{#camelize}}{{name}}{{/camelize}}.Collection= class {{#camelize}}{{name}}{{/camelize}}Collection extends Collection
  model: {{#camelize}}{{name}}{{/camelize}}
  # To sync to localStorage
  # localStorage: 'app.{{#camelize}}{{pluralName}}{{/camelize}}'

  # If you want undoable appEvent commands, uncomment the following:
  # initialize: ->
  #   @crud= UndoManager.crudHelpers this
  # 
  # appEvents:
  #   'add:{{name}}': (data)-> @crud.doAdd data
  #   'remove:{{name}}': (idOrModel)-> @crud.doRemove idOrModel
  #   'update:{{name}}': (idOrModel, data)-> @crud.doUpdate idOrModel, data



module.exports= {{#camelize}}{{name}}{{/camelize}}